import { Button } from "./SubmitButton.styles.js";

const SubmitButton = () => {
  return (
    <Button className={"submit-btn"} type="submit">
      Search
    </Button>
  );
};

export default SubmitButton;
